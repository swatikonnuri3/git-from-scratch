Topic: Interactive Rebase

git rebase -i HEAD~3 → edit last 3 commits

Commands:
pick → keep
squash → combine
reword → rename message
edit → modify commit
drop → delete commit

Used to clean commit history before merging.

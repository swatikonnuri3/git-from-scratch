Topic: Git Stash

git stash → save changes temporarily
git stash list → view stashes
git stash pop → restore and remove
git stash apply → restore but keep
git stash clear → delete all stashes

Used when work is not ready to commit.

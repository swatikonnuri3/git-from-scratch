Topic: Git Tags

git tag → list tags
git tag v1.0 → create lightweight tag
git tag -a v1.1 -m "message" → annotated tag
git push origin --tags → push all tags
git tag -d v1.0 → delete local tag

Tags are used for versioning (v1.0, v2.0).
